import RadioInput from './radio-input.html.twig';
import markdown from './readme.md';

RadioInput.data = {
  name: 'Radio Input',
  other: {
    notes: { markdown }
  },
};

export default RadioInput;
