import CheckboxInput from './checkbox-input.html.twig';
import markdown from './readme.md';

CheckboxInput.data = {
  name: 'Checkbox Input',
  other: {
    notes: { markdown }
  },
};

export default CheckboxInput;
